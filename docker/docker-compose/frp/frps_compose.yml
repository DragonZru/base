# frps_server.yml
services:
  frps:
    image: snowdreamtech/frps:0.62
    volumes:
      - ./frps.toml:/etc/frp/frps.toml
    ports:
      - "7000:7000"
      #- "80:80"
      # web ui
      - "7500:7500"
      - "8081:8081"
    restart: always