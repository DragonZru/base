# frps.toml
bindPort = 7000

auth.method = "token"
auth.token = "bYoqT51-"

# Web 管理面板配置
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin"

#vhostHTTPPort = 80